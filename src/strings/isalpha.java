public class isalpha {
    public static int ft_isalpha(Integer args) {
        if (args == null)
        {
            return -1;
        }
        
        // verificar se o parametro passado não for um caractere alfanumerico (ver na tabela ascii), retornar -1

        return 0;
    }
}

// int isalpha(int argument);